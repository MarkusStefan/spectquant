<prerequisites>

python3 -m pip install --upgrade build

python3 -m pip install --upgrade twine


<run>

PACKAGES/spectquant $ python3 -m build 

PACKAGES/spectquant $ python3 -m twine upload dist/*


<alternatively run for specific package version>

PACKAGES/spectquant $ python3 -m twine upload dist/spectquant-X.Y.ZZ*
